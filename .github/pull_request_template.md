## 这个 PR 做了什么

<!-- 一两句话说清楚。评审的人不该需要读完 diff 才知道你在干嘛。 -->

Closes #

## 怎么验证

<!-- 评审的人照着这几步就能看到你的改动。例如：
1. 后端 ./gradlew bootRun，前端 npm start
2. 打开 /trips/1，拖动第二个景点到第一位
3. 地图上的路线应该跟着重新排
-->

## 截图

<!-- 改了界面就贴一张，改了交互就贴个录屏。这一栏能省掉一半的来回。 -->

## 自查

- [ ] `npm run build` 在本地能通过，控制台没有新的 warning
- [ ] 没有提交 `node_modules/`、`.env.local` 或者 API key
- [ ] 依赖的后端接口已经合进 main 了（没有的话在上面写清楚）
