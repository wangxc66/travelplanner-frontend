OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-sol
# A reasoning model rejects function tools on /v1/chat/completions unless reasoning is off, and the
# assistant's whole tool layer depends on those. Leave empty for a model that does not reason.
OPENAI_REASONING_EFFORT=none
HOST=127.0.0.1
PORT=5001
